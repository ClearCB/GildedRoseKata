package domain;

/**
 * This java class is going to be an interface common to all the
 * items, which we will use a "contract" to use the override
 * propierty to finally use polymorphism and inheritance to the
 * different objects and classes
 */

public interface Updateable {
    
    // This method will be override to all the future clases
    public void updateQuality();

        // Future behaviour of each item at the item
    
}
